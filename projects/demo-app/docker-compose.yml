services:
  demo-app:
    build: .
    container_name: homelab-demo-app
    restart: unless-stopped
    environment:
      - APP_ENV=homelab
      - PORT=3000
    ports:
      - "192.168.1.18:8080:3000"
